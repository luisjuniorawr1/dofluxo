name: dofluxo
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+2
environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flex_color_scheme: ^8.4.0
  google_fonts: ^8.0.2
  responsive_framework: ^1.5.1
  flutter_bloc: ^9.1.1
  percent_indicator: ^4.2.5
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  google_sign_in: ^7.2.0
  cloud_firestore: ^6.5.0
  file_picker: ^11.0.2
  image_picker: ^1.2.2
  palette_generator: ^0.3.3+7
  flutter_colorpicker: ^1.1.0
  mask_text_input_formatter: ^2.9.0
  provider: ^6.1.5+1
  uuid: ^4.5.1
  # Pacote profissional para Kanban multiplataforma (substitui o anterior)
  appflowy_board: ^0.1.2 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true