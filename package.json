{
  "scripts": {
    "deploy": "powershell -ExecutionPolicy Bypass -File .\\deploy-web.ps1",
    "deploy:hosting": "flutter build web && firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^15.22.0"
  }
}
